Read `spec.md` in full before touching any code.

Build exactly what is described — nothing more, nothing less. Do not:
- Add features not listed in the spec
- Invent solutions the spec didn't ask for
- Skip requirements because they seem optional

When the build is complete, go through every numbered requirement in `spec.md` and confirm each one is covered. Format your report as:

- ✅ Requirement N — one-line confirmation
- ❌ Requirement N — what is missing

If anything is missing, fix it before finishing. Only report done when every requirement is marked ✅.
